from flask import Flask, request, jsonify\nfrom datetime import datetime\nfrom typing import Dict, Any\nimport json\n\napp = Flask(__name__)\n\n# In-memory storage for time tracking records\ntime_records = []\n\n@app.route('/api/ponto', methods=['POST'])\ndef register_time():\n    """\n    Endpoint to register electronic time tracking\n    Expected JSON format:\n    {\n        "employee_id": "string",\n        "employee_name": "string",\n        "timestamp": "ISO format datetime (optional, uses current if not provided)",\n        "type": "entrada|saida" (entry or exit)\n    }\n    """\n    try:\n        data = request.get_json()\n        \n        # Validate required fields\n        if not data:\n            return jsonify({"error": "No data provided"}), 400\n        \n        required_fields = ["employee_id", "employee_name", "type"]\n        for field in required_fields:\n            if field not in data:\n                return jsonify({"error": f"Missing required field: {field}"}), 400\n        \n        # Validate type field\n        if data["type"] not in ["entrada", "saida"]:\n            return jsonify({"error": "Type must be 'entrada' or 'saida'"}), 400\n        \n        # Use provided timestamp or current time\n        if "timestamp" in data and data["timestamp"]:\n            try:\n                timestamp = datetime.fromisoformat(data["timestamp"])\n            except ValueError:\n                return jsonify({"error": "Invalid timestamp format. Use ISO format"}), 400\n        else:\n            timestamp = datetime.now()\n        \n        # Create record\n        record = {\n            "id": len(time_records) + 1,\n            "employee_id": data["employee_id"],\n            "employee_name": data["employee_name"],\n            "type": data["type"],\n            "timestamp": timestamp.isoformat(),\n            "registered_at": datetime.now().isoformat()\n        }\n        \n        time_records.append(record)\n        \n        return jsonify({\n            "success": True,\n            "message": "Time tracking registered successfully",\n            "record": record\n        }), 201\n    \n    except Exception as e:\n        return jsonify({"error": str(e)}), 500\n\n@app.route('/api/ponto', methods=['GET'])\ndef get_records():\n    """\n    Endpoint to retrieve all time tracking records\n    """\n    return jsonify({\n        "total": len(time_records),\n        "records": time_records\n    }), 200\n\n@app.route('/api/ponto/<int:record_id>', methods=['GET'])\ndef get_record(record_id):\n    """\n    Endpoint to retrieve a specific time tracking record\n    """\n    for record in time_records:\n        if record["id"] == record_id:\n            return jsonify(record), 200\n    \n    return jsonify({"error": "Record not found"}), 404\n\n@app.route('/api/ponto/employee/<employee_id>', methods=['GET'])\ndef get_employee_records(employee_id):\n    """\n    Endpoint to retrieve all records for a specific employee\n    """\n    employee_records = [r for r in time_records if r["employee_id"] == employee_id]\n    \n    return jsonify({\n        "employee_id": employee_id,\n        "total": len(employee_records),\n        "records": employee_records\n    }), 200\n\n@app.route('/api/health', methods=['GET'])\ndef health_check():\n    """\n    Health check endpoint\n    """\n    return jsonify({"status": "healthy"}), 200\n\n@app.errorhandler(404)\ndef not_found(error):\n    return jsonify({"error": "Endpoint not found"}), 404\n\n@app.errorhandler(405)\ndef method_not_allowed(error):\n    return jsonify({"error": "Method not allowed"}), 405\n\nif __name__ == '__main__':\n    app.run(debug=True, host='0.0.0.0', port=5000)\n